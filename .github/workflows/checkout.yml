name: Checkout Workflow

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: List files
        run: ls

      - name: Read File
        run: cat index.txt
