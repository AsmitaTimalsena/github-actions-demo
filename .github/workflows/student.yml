name: Student Workflow

on:
  push:

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show Student File
        run: cat student.txt

      - name: Print Messages
        run: |
          echo "Welcome"
          echo "Learning GitHub Actions"
          pwd
          ls